apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: backend-production
  labels:
    {{- include "backend.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
